@model IEnumerable<DotNetSheff.MongoDbBlog.Models.Post>

<div>

    @if (Model.Any())
    {
        foreach (var post in Model)
        {
            @Html.Partial("_Post", post)
        
        }
    }
    else
    {
        <p>No posts found!</p>
    }

</div>